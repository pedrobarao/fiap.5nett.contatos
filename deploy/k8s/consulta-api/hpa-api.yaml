apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: consulta-api-hpa
  namespace: contatos-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: consulta-contatos-api
  minReplicas: 1
  maxReplicas: 1
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 2